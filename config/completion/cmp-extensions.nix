{
  plugins = {
    cmp-dictionary = {
      enable = true;
    };
    cmp-nvim-lsp = {
      enable = true;
    }; # lsp
    cmp-nvim-lsp-document-symbol = {
      enable = true;
    };
    cmp-nvim-lsp-signature-help = {
      enable = true;
    };
    cmp-nvim-lua = {
      enable = true;
    };
    cmp-buffer = {
      enable = true;
    };
    cmp-path = {
      enable = true;
    }; # file system paths
    cmp_luasnip = {
      enable = true;
    }; # snippets
    cmp-cmdline = {
      enable = false;
    }; # autocomplete for cmdline
    cmp-treesitter = {
      enable = true;
    };
  };
}
